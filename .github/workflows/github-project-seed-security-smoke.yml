name: GitHub Project Seed Security Smoke Tests

"on":
  workflow_call:
  workflow_dispatch:

jobs:
  smoke-test:
    name: Security Smoke Tests (github-project-seed)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: ".node-version"
          cache: "npm"

      - name: Resolve smoke test endpoint and allowed origin
        env:
          PROJECT_SEED_URL: ${{ secrets.PROJECT_SEED_URL }}
          ALLOWED_ORIGIN: ${{ secrets.ALLOWED_ORIGIN }}
          ALLOWED_ORIGINS: ${{ secrets.ALLOWED_ORIGINS }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
        run: |
          seed_url=""
          if [ -n "${VITE_SUPABASE_URL:-}" ]; then
            seed_url="${VITE_SUPABASE_URL%/}/functions/v1/github-project-seed"
          else
            seed_url="${PROJECT_SEED_URL:-}"
          fi

          allowed_origin="$(
            ALLOWED_ORIGIN="${ALLOWED_ORIGIN:-}" \
              ALLOWED_ORIGINS="${ALLOWED_ORIGINS:-}" \
              node scripts/infra/resolve-smoke-allowed-origin.mjs
          )"

          {
            echo "GITHUB_SEED_URL=$seed_url"
            echo "ALLOWED_ORIGIN=$allowed_origin"
          } >> "$GITHUB_ENV"

      - name: Validate required security test secrets
        env:
          GITHUB_SEED_URL: ${{ env.GITHUB_SEED_URL }}
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          missing=""
          for key in GITHUB_SEED_URL SUPABASE_URL SUPABASE_ANON_KEY SUPABASE_SERVICE_ROLE_KEY; do
            if [ -z "${!key}" ]; then
              missing="$missing $key"
            fi
          done

          if [ -n "$missing" ]; then
            echo "Missing required secrets:$missing"
            echo "Set the listed missing key(s) above as repository secrets before running this workflow."
            echo "Required secret mappings:"
            echo "- GITHUB_SEED_URL env: secrets.PROJECT_SEED_URL (fallback: secrets.VITE_SUPABASE_URL + /functions/v1/github-project-seed)"
            echo "- SUPABASE_URL: secrets.VITE_SUPABASE_URL"
            echo "- SUPABASE_ANON_KEY: secrets.VITE_SUPABASE_ANON_KEY"
            echo "- SUPABASE_SERVICE_ROLE_KEY: secrets.SUPABASE_SERVICE_ROLE_KEY"
            exit 1
          fi

      - name: Install dependencies
        run: npm ci

      - name: Run github-project-seed security smoke tests
        env:
          GITHUB_SEED_URL: ${{ env.GITHUB_SEED_URL }}
          ALLOWED_ORIGIN: ${{ env.ALLOWED_ORIGIN }}
          ALLOWED_ORIGINS: ${{ secrets.ALLOWED_ORIGINS }}
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npm run security:test:github-seed
