---
description: Flag skill and rule gaps after UI, motion, shell, and agent-instruction work
alwaysApply: true
---

# Improvement Flagging

After completing UI, motion, shell layout, design tokens in styles, or edits to `.cursor/skills/` / `.cursor/rules/`:

1. End the response with a **`## Improvement flags`** section (use `- [none]` when nothing applies).
2. Flag **class-of-bug** fixes on first occurrence — not only after the pattern repeats.
3. Load `.cursor/skills/meta/improvement-protocol/SKILL.md` when drafting flags or implementing doc updates.
4. **Do not** implement skill/rule file changes unless the user approved (parallel session or explicit request).

Portable lessons → `~/.cursor/skills/code/**`. Project contracts → repo `.cursor/skills/project/**`.
